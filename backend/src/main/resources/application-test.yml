# =====================================================
# ./gradlew test 시 동작할 DB설정(--spring.profiles.active=test인 경우 동작하는 설정)
# =====================================================
spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
